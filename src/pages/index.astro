---
import { getEntry } from 'astro:content';
import Layout from "../layouts/Base.astro";

const { render } = await getEntry('copy', '2024-07-14-homecopy');
const { Content } = await render();
---

<Layout>
    <Content />
</Layout>
